# URL del backend. En desarrollo se usa http://localhost:8080 si no se define.
# En producción (Railway/Vercel) apunta a la URL pública del backend, ej:
# VITE_API_URL=https://transporterayza-backend.up.railway.app
VITE_API_URL=
